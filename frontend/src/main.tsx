import React,{useEffect,useRef,useState} from 'react';
import {createRoot} from 'react-dom/client';
import {MapContainer,TileLayer,CircleMarker,Popup,useMap} from 'react-leaflet';
import {LineChart,Line,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';
import 'leaflet/dist/leaflet.css';import './style.css';
const API_ROOT=(import.meta.env.VITE_API_URL||'https://alphax-backend-dexi.onrender.com').replace(/\/$/,'');const API=API_ROOT+'/api';type Any=any;
const fmt=(s:string)=>s?new Date(s).toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',second:'2-digit'}):'—';
async function get(p:string){const r=await fetch(API+p);if(!r.ok)throw Error(await r.text());return r.json()}
function key(){try{const k='alphax_visitor_id';let id=localStorage.getItem(k);if(id)return id;id='V'+(crypto.randomUUID?crypto.randomUUID().replaceAll('-',''):Math.random().toString(36).slice(2)).slice(0,9).toUpperCase();try{localStorage.setItem(k,id)}catch{}return id}catch{return 'V'+Math.random().toString(36).slice(2,11).toUpperCase()}}
async function context(){const ua=(navigator as any).userAgentData;const base={user_agent:navigator.userAgent};if(!ua)return base;try{const h=await ua.getHighEntropyValues(['model','platform','uaFullVersion']);return {...base,device_type:ua.mobile?'Mobile':'Desktop',device_name:h.model||undefined,os_hint:h.platform||undefined,browser_version:h.uaFullVersion||undefined}}catch{return base}}
function Badge({children}:{children:React.ReactNode}){return <span className="badge">{children}</span>}
function LiveClock(){const [now,setNow]=useState(()=>new Date());useEffect(()=>{const timer=window.setInterval(()=>setNow(new Date()),1000);return()=>window.clearInterval(timer)},[]);return <div className="live-clock"><b>LIVE TIME</b><strong>{now.toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',second:'2-digit',hour12:true})}</strong></div>}
function Move({v}:{v:Any}){const m=useMap();useEffect(()=>{if(v?.latitude!=null&&v?.longitude!=null)m.setView([v.latitude,v.longitude],14)},[v,m]);return null}
function MapView({items,selected}:{items:Any[],selected:Any}){return <div className="map"><MapContainer center={[11.0168,76.9558]} zoom={11} scrollWheelZoom={false} style={{height:'100%',width:'100%'}}><Move v={selected}/><TileLayer attribution="&copy; OpenStreetMap" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/>{items.filter(v=>v.latitude!=null&&v.longitude!=null).map(v=><CircleMarker key={v.visitor_id} center={[v.latitude,v.longitude]} radius={v.visitor_id===selected?.visitor_id?12:8} pathOptions={{color:v.visitor_type==='IOT'?'green':v.location_source==='GPS'?'teal':'purple'}}><Popup><b>{v.visitor_type==='IOT'?'IOT DEVICE':'LIVE BROWSER VISITOR'}</b><br/>{v.name}<br/>Device: {v.device_name||v.device_type||'Unknown'}<br/>Browser: {v.browser||'—'}<br/>OS: {v.os||v.operating_system||'—'}<br/>Location: {v.visitor_type==='IOT'?'GPS':v.location_source==='GPS'?'GPS':'Approximate IP'}<br/>{v.latitude.toFixed(5)}, {v.longitude.toFixed(5)}{v.location_accuracy!=null&&<> · ±{v.location_accuracy.toFixed(0)}m</>}<br/>Status: {v.online?'ONLINE':'OFFLINE'}<br/>Last seen: {fmt(v.last_seen)}</Popup></CircleMarker>)}</MapContainer><small>Green IoT GPS · teal browser GPS · purple approximate IP</small></div>}
function LocationAccess(){const [status,setStatus]=useState('APPROXIMATE IP LOCATION');const [accuracy,setAccuracy]=useState<number|null>(null);const watch=useRef<number|null>(null);const send=async(gps:Any)=>{try{await fetch(API+'/visitor/heartbeat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({visitor_id:key(),...(await context()),...gps})})}catch{}};const start=()=>{if(!navigator.geolocation){setStatus('GPS UNAVAILABLE');return}setStatus('REQUESTING');watch.current=navigator.geolocation.watchPosition(p=>{const gps={latitude:p.coords.latitude,longitude:p.coords.longitude,location_accuracy:p.coords.accuracy,location_source:'GPS'};setAccuracy(p.coords.accuracy);setStatus('GPS ACTIVE');send(gps)},e=>setStatus(e.code===1?'GPS PERMISSION DENIED':'GPS UNAVAILABLE'),{enableHighAccuracy:true,maximumAge:5000,timeout:10000})};useEffect(()=>{let cancelled=false;const check=async()=>{if(!navigator.geolocation){setStatus('GPS UNAVAILABLE');return}try{if('permissions' in navigator){const permission=await navigator.permissions.query({name:'geolocation' as PermissionName});if(cancelled)return;if(permission.state==='denied'){setStatus('GPS PERMISSION DENIED');return}permission.onchange=()=>{if(permission.state==='granted'&&!cancelled&&!watch.current)start()}}start()}catch{start()}};check();return()=>{cancelled=true;if(watch.current!=null)navigator.geolocation.clearWatch(watch.current)}},[]);return <div className="location-access"><b>LOCATION</b><span>{status==='GPS ACTIVE'?`GPS ACTIVE · Accuracy ${accuracy?.toFixed(0)}m`:status}</span><small>{status==='GPS PERMISSION DENIED'||status==='GPS UNAVAILABLE'?'Using approximate IP location.':'Browser GPS is requested automatically; exact location is used only while permission is enabled.'}</small></div>}
function TransactionMonitor(){const [amount,setAmount]=useState('');const [r,setR]=useState<Any>();const [busy,setBusy]=useState(false);async function run(){const n=Number(amount.replace(/[^0-9.]/g,''));if(!n)return;setBusy(true);try{const x=await fetch(API+'/transactions/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({amount:n})});setR(await x.json())}catch{setR({error:'Backend unavailable'})}finally{setBusy(false)}}return <section className="card"><b>TRANSACTION MONITOR</b><div className="form"><input inputMode="decimal" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="₹ amount"/><button onClick={run}>{busy?'...':'ANALYZE'}</button></div>{r&&!r.error&&<p><strong>{r.status}</strong> · ₹{Number(r.amount).toLocaleString('en-IN')} · Risk {r.risk_score} · <Badge>{r.decision}</Badge> · {r.alert?'Alert generated':'No alert'}</p>}</section>}
function Dashboard(){const [data,setData]=useState<Any>({});const [selectedId,setSelectedId]=useState<string|null>(null);const [selected,setSelected]=useState<Any>();const [health,setHealth]=useState(true);const [visitorStatus,setVisitorStatus]=useState('CONNECTING');const run=useRef(false);const gps=useRef<Any>();const heartbeat=async()=>{try{const r=await fetch(API+'/visitor/heartbeat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({visitor_id:key(),...(await context()),...(gps.current||{})}),signal:AbortSignal.timeout(5000)});setVisitorStatus(r.ok?'ONLINE':'RECONNECTING')}catch{setVisitorStatus('RECONNECTING')}};const refresh=async()=>{if(run.current)return;run.current=true;const paths=['/health','/dashboard/summary','/admin/live-visitors','/transactions?limit=8','/alerts','/dashboard/timeline'];const q=await Promise.allSettled(paths.map(get));const v=(i:number)=>q[i].status==='fulfilled'?(q[i] as PromiseFulfilledResult<Any>).value:null;setHealth(!!v(0));setData((old:Any)=>({...old,summary:v(1)||old.summary,visitors:v(2)||old.visitors||[],tx:v(3)||old.tx||[],alerts:v(4)||old.alerts||[],timeline:v(5)||old.timeline||[]}));run.current=false};useEffect(()=>{heartbeat();const h=setInterval(heartbeat,5000);refresh();const p=setInterval(refresh,2000);return()=>{clearInterval(h);clearInterval(p)}},[]);useEffect(()=>{if(!selectedId){setSelected(null);return}let busy=false;const load=async()=>{if(busy)return;busy=true;try{setSelected(await get('/admin/live-visitors/'+encodeURIComponent(selectedId)))}finally{busy=false}};load();const p=setInterval(load,2000);return()=>clearInterval(p)},[selectedId]);const items=data.visitors||[];const iot=items.filter((v:Any)=>v.visitor_type==='IOT');const web=items.filter((v:Any)=>v.visitor_type==='ONLINE');return <><header><h1>AlphaX-IoT</h1><LiveClock/><span>{health?'BACKEND ONLINE':'BACKEND OFFLINE'}</span></header><main><div className="connection">VISITOR CONNECTION: <b>{visitorStatus}</b></div><LocationAccess/><TransactionMonitor/><section className="stats"><div><b>TRANSACTIONS</b><strong>{data.summary?.total_transactions||0}</strong></div><div><b>HIGH RISK</b><strong>{data.summary?.high_risk_transactions||0}</strong></div><div><b>LIVE VISITORS</b><strong>{iot.length+web.length}</strong><small>IoT: {iot.length} · Online: {web.length}</small></div><div><b>TAMPER</b><strong>{data.summary?.tamper_alerts||0}</strong></div></section><section className="card"><h2>Live visitor map</h2><MapView items={items} selected={selected}/></section><section className="card"><h2>Connected assets</h2>{selected&&<div className="tracking"><b>LIVE TRACKING · {selected.name}</b><br/>Status: {selected.online?'ONLINE':'OFFLINE'}<br/>Source: {selected.visitor_type==='IOT'?'GPS':selected.location_source==='GPS'?'GPS':'Approximate IP'}<br/>{selected.latitude},{selected.longitude}{selected.location_accuracy!=null&&` · Accuracy ${selected.location_accuracy.toFixed(0)}m`}<br/>Last seen: {fmt(selected.last_seen)}</div>}{items.map((v:Any)=><div className={'visitor '+(v.visitor_id===selectedId?'selected':'')} key={v.visitor_id}><span><b>{v.name}</b><small>{v.visitor_type} · {v.device_name||v.device_type} · {v.browser||v.os||''} · {v.online?'ONLINE':'OFFLINE'}</small></span><button onClick={()=>setSelectedId(selectedId===v.visitor_id?null:v.visitor_id)}>{selectedId===v.visitor_id?'STOP TRACKING':'TRACK'}</button></div>)}</section><section className="card"><h2>Recent alerts</h2>{(data.alerts||[]).slice(0,8).map((a:Any)=><p key={a.id}><b>{a.title}</b> · {a.message}</p>)}</section><section className="card"><h2>Risk timeline</h2><ResponsiveContainer width="100%" height={180}><LineChart data={data.timeline||[]}><XAxis dataKey="time"/><YAxis/><Tooltip/><Line dataKey="risk" stroke="blue"/></LineChart></ResponsiveContainer></section></main></>}
function App(){return <Dashboard/>}createRoot(document.getElementById('root')!).render(<App/>);
